.contents
  .contents__prefecture.form-group
    = form_with url: prefectures_posts_path, method: :get, class: 'contents__prefecture__Form' do |form|
      = form.collection_select :tag_id, Prefecture.all, :id, :name, {prompt: "検索したい都道府県を選択してください"}, onchange: 'submit(this.form);'
    - if params[:tag_id].present?
      .tag-index
        = @tag.name
        の記事一覧
      - @posts.each do |post|
        = render partial: "post", locals: { post: post }
      = paginate(@posts)